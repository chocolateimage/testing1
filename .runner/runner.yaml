events:
  push:
    tasks: ["check"]

tasks:
  check:
    depends:
      - test
    build: .runner/check.Dockerfile
    exports:
      - /app/checks
    release:
      - /app/release
    prepare: plain
