events:
  push:
    tasks: ["check", "useless"]

tasks:
  check:
    depends:
      - test
    build: .runner/check.Dockerfile
    exports:
      - /app/checks
    release:
      - /app/release
    prepare: plain
  useless:
    build: .runner/useless.Dockerfile