events:
  push:
    tasks: ["check"]

tasks:
  check:
    depends:
      - test
    build: .runner/useless.Dockerfile
    exports:
      - /app/checks
    release:
      - /app/release
    prepare: plain
